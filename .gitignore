# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vercel
.vinxi
.tanstack/**
.nitro
*.local

# Env
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Temporary binaries (e.g. cloudflared, downloaded directly rather than via
# npm since the npx-wrapped package's postinstall binary fetch is blocked by
# this repo's allow-scripts policy)
.tmp-bin/

# Claude Code local tooling config (personal/machine-specific, not app code)
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env*
